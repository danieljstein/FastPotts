% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_basis_log_density_field.R
\name{spatial_basis_log_density_field}
\alias{spatial_basis_log_density_field}
\title{Fit a log-linear spatial transcript density field}
\usage{
spatial_basis_log_density_field(
  transcripts_df,
  posterior = NULL,
  return_density = !is.null(posterior),
  basis = c("3d", "2d", "tri", "bcc"),
  s,
  x = "x_location",
  y = "y_location",
  z = "z_location",
  origin = NULL,
  quadrature_subdivision = 4L,
  store_quadrature_coords = FALSE,
  lambda = 0.1,
  regularization = c("bounded", "quadratic", "huber"),
  delta = 1,
  sigma = 1,
  lambda_laplacian = 0,
  maxit = 100L,
  reltol = 1e-06,
  n_threads = NULL,
  show_progress = TRUE
)
}
\arguments{
\item{transcripts_df}{Transcript-level data frame.}

\item{posterior}{Optional numeric transcript-by-cell-type posterior matrix.
If supplied, rows are normalized and used only to decompose the fitted
total density into cell-type-specific densities.}

\item{return_density}{Logical; if \code{TRUE} and \code{posterior} is supplied, return
the transcript-by-cell-type density matrix \code{total_density * posterior}.
Set to \code{FALSE} for large datasets when only \code{total_density} is needed.}

\item{basis}{Character; \code{"2d"}/\code{"tri"} or \code{"3d"}/\code{"bcc"}.}

\item{s}{Positive spatial basis mesh size.}

\item{x, y, z}{Character coordinate column names.}

\item{origin}{Optional lattice origin.}

\item{quadrature_subdivision}{Positive integer subdivision factor for
occupied-simplex quadrature.}

\item{store_quadrature_coords}{Logical; if \code{TRUE}, store quadrature point
coordinates in the returned object. The fitting objective does not need
these coordinates, so the default \code{FALSE} is more memory efficient for
large 3D datasets.}

\item{lambda}{Non-negative smoothing strength on neighboring basis-point
log-density slopes.}

\item{regularization}{Character; one of \code{"quadratic"}, \code{"huber"}, or
\code{"bounded"}.}

\item{delta}{Positive Huber transition scale. Used only when
\code{regularization = "huber"}.}

\item{sigma}{Positive bounded-penalty slope scale. Used only when
\code{regularization = "bounded"}.}

\item{lambda_laplacian}{Non-negative strength for a graph Laplacian
curvature penalty on the basis-point log-density field. This penalizes
deviations from a distance-weighted neighbor average.}

\item{maxit}{Maximum L-BFGS iterations.}

\item{reltol}{Relative convergence tolerance.}

\item{n_threads}{Integer number of OpenMP threads. If \code{NULL}, uses runtime
default.}

\item{show_progress}{Logical; print progress messages.}
}
\value{
A list with fitted total density, optional cell-type density,
fitted basis coefficients, quadrature design, basis design, optimizer
result, and effective parameters.
}
\description{
Fits a simplified inhomogeneous Poisson point-process density model on the
same triangular or BCC barycentric basis used by
\code{\link[=spatial_basis_segmentation]{spatial_basis_segmentation()}}. The model has no boundary attenuation terms:
}
\details{
\deqn{
\rho(x) = \exp(\eta(x)), \quad
\eta(x) = \sum_a \lambda_a(x) w_a.
}

The integral term is approximated by simplex-local quadrature over occupied
simplexes. If \code{posterior} is supplied, transcript-level cell-type-specific
densities are returned as \code{total_density * posterior}.
}
