% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{potts_lbp_cpp}
\alias{potts_lbp_cpp}
\title{Loopy belief propagation for a Potts model on a sparse directed graph}
\usage{
potts_lbp_cpp(
  adj_ptr,
  adj_idx,
  rev_idx,
  edge_weights,
  node_potential,
  max_iter = 50L,
  damping = 1,
  tol = 0.01,
  synchronous = TRUE
)
}
\arguments{
\item{adj_ptr}{Integer vector of length \code{N + 1} giving CSR row pointers for
the directed adjacency structure. Directed neighbors of node \code{u} are stored
in \code{adj_idx[adj_ptr[u] : adj_ptr[u + 1] - 1]} using 0-based C++ indexing.}

\item{adj_idx}{Integer vector of length \code{E} containing the destination node
for each directed edge. Values are expected to be 0-based node indices.}

\item{rev_idx}{Integer vector of length \code{E} such that \code{rev_idx[e]} is the
directed-edge index of the reverse of edge \code{e}.}

\item{edge_weights}{Numeric vector of length \code{E} containing log same-label
Potts weights for each directed edge.}

\item{node_potential}{Numeric matrix of dimension \verb{N x K} containing log
unary potentials.}

\item{max_iter}{Maximum number of LBP sweeps.}

\item{damping}{Damping parameter in \verb{(0, 1]}. Values below 1 blend each new
message with the previous message to improve stability on loopy graphs.
Default is \code{1.0}, corresponding to no damping.}

\item{tol}{Convergence tolerance. Iteration stops early when the maximum
absolute message change in a sweep is below \code{tol}. Default is \code{1e-2}.}

\item{synchronous}{Logical; if \code{TRUE} (default), uses synchronous updates
with a second message buffer. If \code{FALSE}, uses in-place asynchronous
updates.}
}
\value{
A list with components:
\describe{
\item{marginals}{\verb{N x K} numeric matrix of approximate node marginals.}
\item{iterations}{Number of sweeps performed.}
\item{max_delta}{Maximum absolute message change in the final sweep.}
}
}
\description{
Runs sum-product loopy belief propagation (LBP) for a Potts model with
node-specific unary log-potentials and edge-specific pairwise Potts weights.
}
\details{
The graph is supplied in CSR-like directed adjacency form. Each undirected
edge should appear twice in \code{adj_idx}: once as \code{u -> v} and once as
\code{v -> u}. The vector \code{rev_idx} maps each directed edge to the index of its
reverse directed edge.

The pairwise potential is parameterized as
\deqn{
\psi_{uv}(a,b) = \exp(w_{uv}) \text{ if } a=b,\quad 1 \text{ otherwise},
}
where \verb{edge_weights[edge] = w_\{uv\}} is the log same-label preference for the
directed edge \code{u -> v}. In most applications the two directions of an
undirected edge will have the same weight.

The unary potential is parameterized by \code{node_potential}, whose \verb{(i, k)} entry
is the log unary potential for node \code{i} and label \code{k}.

Message updates are performed in log-space for numerical stability. The
returned node marginals are normalized probabilities on the original scale.

Let
\deqn{
H_i(k) = \log \phi_i(k) + \sum_{\ell \in N(i)} \log m_{\ell \to i}(k).
}
For the directed edge \code{u -> v}, the cavity field is
\deqn{
h_{u \to v}(k) = H_u(k) - \log m_{v \to u}(k).
}
With the Potts parameterization above, the message update simplifies to
\deqn{
m_{u \to v}(k) \propto S_{u \to v} +
  (\exp(w_{uv}) - 1)\exp(h_{u \to v}(k)),
}
where
\deqn{
S_{u \to v} = \sum_a \exp(h_{u \to v}(a)).
}
This reduces each per-edge update from \code{O(K^2)} for a general pairwise model
to \code{O(K)} for the Potts model.
}
\examples{
\dontrun{
# Suppose graph contains directed edges for both directions of each
# undirected edge, stored in CSR form.
fit <- potts_lbp_cpp(
  adj_ptr = adj_ptr,
  adj_idx = adj_idx,
  rev_idx = rev_idx,
  edge_weights = edge_weights,
  node_potential = node_potential
)

fit$marginals
fit$iterations
fit$max_delta

# Example with non-default controls
fit2 <- potts_lbp_cpp(
  adj_ptr = adj_ptr,
  adj_idx = adj_idx,
  rev_idx = rev_idx,
  edge_weights = edge_weights,
  node_potential = node_potential,
  max_iter = 100,
  damping = 0.5,
  tol = 1e-4,
  synchronous = FALSE
)
}

}
