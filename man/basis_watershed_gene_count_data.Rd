% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basis_watershed_partitioning.R
\name{basis_watershed_gene_count_data}
\alias{basis_watershed_gene_count_data}
\title{Collect basis-watershed gene counts with cell and transcript metadata}
\usage{
basis_watershed_gene_count_data(
  basis_partition,
  transcripts_df,
  posterior = NULL,
  density_fit = NULL,
  gene = "feature_name",
  x = "x_location",
  y = "y_location",
  z = "z_location",
  mode = c("max_posterior", "weighted", "density"),
  cell_type_groups = partition_cell_type_groups(basis_partition)
)
}
\arguments{
\item{basis_partition}{Result from \code{\link[=partition_basis_watershed_initial]{partition_basis_watershed_initial()}} or
\code{\link[=partition_basis_watershed_total]{partition_basis_watershed_total()}}.}

\item{transcripts_df}{Transcript-level data frame corresponding to the
density and segmentation fits.}

\item{posterior}{Optional transcript-by-cell-type posterior matrix. Required
for \code{mode = "max_posterior"} and \code{mode = "weighted"}.}

\item{density_fit}{Optional density fit used to estimate basis-domain
area/volume for each cell. Each active density-domain simplex is split
fractionally across active cell types using the average active cell-type
support of its vertices; each type-specific fraction is then assigned to
that type's highest-density active basin for the simplex.}

\item{gene}{Character gene column name.}

\item{x, y, z}{Character coordinate column names.}

\item{mode}{Character count/assignment mode passed to
\code{\link[=basis_watershed_gene_counts]{basis_watershed_gene_counts()}}.}

\item{cell_type_groups}{Optional named list mapping coarse watershed cell
type names to fine posterior column names. Defaults to the mapping stored
in \code{basis_partition}, if present.}
}
\value{
A list with \code{counts}, \code{cell_metadata}, \code{transcript_cells}, and
\code{domain_metadata}. \code{domain_metadata} contains the total density-domain
area/volume and the active-domain area/volume assigned across watershed
cells.
\code{transcript_cells} contains \code{transcript_row}, \code{cell},
\code{assignment_weight}, \code{cell_type_posterior}, the gene column, and available
coordinate columns.
}
\description{
Returns the sparse gene-by-cell count matrix together with transcript-level
cell assignments and per-cell spatial metadata for plotting.
}
