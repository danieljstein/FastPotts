% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmentation.R
\name{run_crf}
\alias{run_crf}
\title{Cell Type Assignment via Conditional Random Field}
\usage{
run_crf(
  transcripts_df,
  cell_signatures,
  x = "x_location",
  y = "y_location",
  z = "z_location",
  gene = "feature_name",
  qv = "qv",
  qv_threshold = 20,
  n_neighbors = 10,
  dist_threshold = 2,
  same_label_ratio = 5,
  ...
)
}
\arguments{
\item{transcripts_df}{A data frame containing transcript-level data with
columns for coordinates, gene names, and quality values.}

\item{cell_signatures}{A numeric matrix (gene x cell types) of probabilities
or likelihoods for each gene within a cell type. Columns should sum to 1.}

\item{x}{Character; column name for x-coordinates (default: "x_location").}

\item{y}{Character; column name for y-coordinates (default: "y_location").}

\item{z}{Character; column name for z-coordinates (default: "z_location").}

\item{gene}{Character; column name for gene/feature identifiers (default: "feature_name").}

\item{qv}{Character; column name for quality values (default: "qv").}

\item{qv_threshold}{Numeric; minimum quality value to retain transcripts (default: 20).}

\item{n_neighbors}{Integer; number of nearest neighbors to connect (default: 10).}

\item{dist_threshold}{Numeric; maximum spatial distance for edge inclusion (default: 2).}

\item{same_label_ratio}{Numeric; log-probability ratio for same vs. different labels
in the pairwise potential (default: 5).}

\item{...}{Additional arguments passed to \code{potts_lbp_parallel_cpp()}.}
}
\value{
A list containing:
\item{marginals}{Matrix of posterior marginal probabilities (transcripts x cell types).}
\item{transcripts_df}{The input data frame with added \code{label} column containing
assigned cell type labels.}
}
\description{
Assigns cell type labels to individual transcripts using a Potts model
conditional random field (CRF) with loopy belief propagation inference.
}
\details{
This function performs spatial cell type assignment by:
\enumerate{
\item Filtering transcripts by quality threshold
\item Computing Hilbert-curve ordering for spatial coherence
\item Building a k-nearest neighbor graph with spatial distance constraints
\item Running loopy belief propagation on the Potts model to infer cell types
}

The graph structure encodes both spatial proximity and the prior probability
of transcripts sharing the same cell type label.
}
\examples{
\dontrun{
# Assuming transcripts_df and cell_signatures are prepared
result <- run_crf(
  transcripts_df,
  cell_signatures,
  qv_threshold = 20,
  n_neighbors = 10
)
head(result$transcripts_df)
}

}
