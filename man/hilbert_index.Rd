% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hilbert.R
\name{hilbert_index}
\alias{hilbert_index}
\title{Compute Hilbert indices for 2D or 3D coordinates, scaled to the unit cube.}
\usage{
hilbert_index(coords, level = NULL, return_scaled = FALSE)
}
\arguments{
\item{coords}{A numeric matrix with 2 or 3 columns, containing the coordinates
for which to compute Hilbert indices.}

\item{level}{The number of bits to use for each coordinate. If \code{NULL}, uses the maximum possible level that
can be represented as a double (i.e. \code{floor(53 / d)} where \code{d} is the number of dimensions).}

\item{return_scaled}{If \code{TRUE}, returns a list with components \code{hilbert_index} and \code{coords_scaled},
where \code{hilbert_index} is the Hilbert index computed from the scaled coordinates,
and \code{coords_scaled} is the input coordinates scaled to the unit cube according to the specified level.
If \code{FALSE}, returns just the Hilbert index.}
}
\description{
Compute Hilbert indices for 2D or 3D coordinates, scaled to the unit cube.
}
