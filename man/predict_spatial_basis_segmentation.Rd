% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_basis_segmentation.R
\name{predict_spatial_basis_segmentation}
\alias{predict_spatial_basis_segmentation}
\title{Reconstruct outputs from a spatial basis segmentation fit}
\usage{
predict_spatial_basis_segmentation(
  fit,
  transcripts_df = fit$transcripts_df,
  what = c("marginals", "spatial_prior", "logits", "labels", "max_posterior"),
  x = fit$parameters$x \%||\% "x_location",
  y = fit$parameters$y \%||\% "y_location",
  z = fit$parameters$z \%||\% "z_location",
  gene = fit$parameters$gene \%||\% "feature_name",
  n_threads = NULL
)
}
\arguments{
\item{fit}{Result from \code{\link[=spatial_basis_segmentation]{spatial_basis_segmentation()}}.}

\item{transcripts_df}{Transcript-level data frame. Defaults to
\code{fit$transcripts_df}.}

\item{what}{Character vector of outputs to compute. Options are
\code{"marginals"}, \code{"spatial_prior"}, \code{"logits"}, \code{"labels"}, and
\code{"max_posterior"}.}

\item{x}{Character; column name for x-coordinates. Defaults to the fit
parameter, or \code{"x_location"} for older fits.}

\item{y}{Character; column name for y-coordinates. Defaults to the fit
parameter, or \code{"y_location"} for older fits.}

\item{z}{Character; column name for z-coordinates. Defaults to the fit
parameter, or \code{"z_location"} for older fits.}

\item{gene}{Character; column name for gene identifiers. Defaults to the fit
parameter, or \code{"feature_name"} for older fits.}

\item{n_threads}{Integer number of OpenMP threads for prediction. If \code{NULL},
uses the OpenMP runtime default.}
}
\value{
A list containing the requested outputs.
}
\description{
Computes derived transcript-level outputs from a fitted
\code{\link[=spatial_basis_segmentation]{spatial_basis_segmentation()}} object. This is useful for compact fits where
large matrices such as \code{marginals}, \code{spatial_prior}, or \code{logits} were not
returned during fitting.
}
