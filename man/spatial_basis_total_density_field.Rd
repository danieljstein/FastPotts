% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_basis_total_density_field.R
\name{spatial_basis_total_density_field}
\alias{spatial_basis_total_density_field}
\title{Fit total transcript density on an existing spatial basis}
\usage{
spatial_basis_total_density_field(
  segmentation_fit,
  transcripts_df = segmentation_fit$transcripts_df,
  basis = NULL,
  s = NULL,
  x = "x_location",
  y = "y_location",
  z = "z_location",
  origin = NULL,
  basis_subdivision = 1L,
  quadrature_subdivision = 1L,
  store_quadrature_coords = FALSE,
  lambda = 0.1,
  regularization = c("bounded", "quadratic", "huber"),
  delta = 1,
  sigma = 1,
  lambda_laplacian = 0,
  maxit = 100L,
  reltol = 1e-06,
  n_threads = NULL,
  show_progress = TRUE
)
}
\arguments{
\item{segmentation_fit}{Result from \code{\link[=spatial_basis_segmentation]{spatial_basis_segmentation()}} containing
\code{basis_lattice}, \code{basis_points}, and preferably \code{basis_edges}.}

\item{transcripts_df}{Transcript-level data frame. Defaults to
\code{segmentation_fit$transcripts_df}.}

\item{basis}{Character; \code{"2d"}/\code{"tri"} or \code{"3d"}/\code{"bcc"}. If \code{NULL}, inferred
from the number of basis-point coordinate columns.}

\item{s}{Positive spatial basis mesh size. If \code{NULL}, inferred from
\code{segmentation_fit$basis_edges}.}

\item{x, y, z}{Character coordinate column names.}

\item{origin}{Optional lattice origin. If \code{NULL}, inferred from
\code{basis_lattice}, \code{basis_points}, and \code{s}.}

\item{basis_subdivision}{Positive integer refinement factor for the density
basis relative to the segmentation basis. The density mesh size is
\code{s / basis_subdivision}. The integration domain is still defined by the
occupied simplexes of the segmentation basis, so child regions with no
transcripts still contribute to the Poisson integral.}

\item{quadrature_subdivision}{Positive integer subdivision factor for
quadrature within each density-basis subdivision. The effective quadrature
subdivision of each occupied segmentation simplex is
\code{basis_subdivision * quadrature_subdivision}.}

\item{store_quadrature_coords}{Logical; if \code{TRUE}, store quadrature point
coordinates in the returned object. The fitting objective does not need
these coordinates, so the default \code{FALSE} is more memory efficient.}

\item{lambda}{Non-negative smoothing strength on neighboring basis-point
log-density slopes.}

\item{regularization}{Character; one of \code{"quadratic"}, \code{"huber"}, or
\code{"bounded"}.}

\item{delta}{Positive Huber transition scale. Used only when
\code{regularization = "huber"}.}

\item{sigma}{Positive bounded-penalty slope scale. Used only when
\code{regularization = "bounded"}.}

\item{lambda_laplacian}{Non-negative strength for a graph Laplacian
curvature penalty on the basis-point log-density field.}

\item{maxit}{Maximum L-BFGS iterations.}

\item{reltol}{Relative convergence tolerance.}

\item{n_threads}{Integer number of OpenMP threads. If \code{NULL}, uses runtime
default.}

\item{show_progress}{Logical; print progress messages.}
}
\value{
A list with fitted transcript-level and basis-point total densities,
fitted basis coefficients, quadrature design, reused basis design,
optimizer result, and effective parameters.
}
\description{
Fits the same log-linear inhomogeneous Poisson density model as
\code{\link[=spatial_basis_log_density_field]{spatial_basis_log_density_field()}}, but reuses the basis points from an
existing \code{\link[=spatial_basis_segmentation]{spatial_basis_segmentation()}} result. The model ignores cell types
and estimates one total transcript density field:
}
\details{
\deqn{
\rho(x) = \exp(\eta(x)), \quad
\eta(x) = \sum_a \lambda_a(x) w_a.
}
}
