% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_basis_log_density_field.R
\name{predict_spatial_basis_log_density_field}
\alias{predict_spatial_basis_log_density_field}
\title{Reconstruct outputs from a spatial log-density fit}
\usage{
predict_spatial_basis_log_density_field(
  fit,
  transcripts_df = NULL,
  posterior = NULL,
  what = c("total_density", "eta", "density"),
  x = fit$parameters$x \%||\% "x_location",
  y = fit$parameters$y \%||\% "y_location",
  z = fit$parameters$z \%||\% "z_location",
  n_threads = NULL
)
}
\arguments{
\item{fit}{Result from \code{\link[=spatial_basis_log_density_field]{spatial_basis_log_density_field()}}.}

\item{transcripts_df}{Optional transcript-level data frame. If \code{NULL}, uses
\code{fit$transcript_basis_id} and \code{fit$transcript_basis_weight} for the
original fitted transcript locations.}

\item{posterior}{Optional transcript-by-cell-type posterior matrix. Required
when requesting \code{"density"} unless \code{fit$posterior} was stored.}

\item{what}{Character vector of outputs to compute. Options are
\code{"total_density"}, \code{"eta"}, and \code{"density"}.}

\item{x, y, z}{Character coordinate column names used when \code{transcripts_df} is
supplied.}

\item{n_threads}{Integer number of OpenMP threads for prediction. If \code{NULL},
uses the OpenMP runtime default.}
}
\value{
A list containing the requested outputs.
}
\description{
Computes derived transcript-level outputs from a fitted
\code{\link[=spatial_basis_log_density_field]{spatial_basis_log_density_field()}} object. This is useful for compact fits
where \code{total_density}, \code{eta}, or cell-type \code{density} were not returned during
fitting.
}
