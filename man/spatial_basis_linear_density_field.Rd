% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_basis_linear_density_field.R
\name{spatial_basis_linear_density_field}
\alias{spatial_basis_linear_density_field}
\title{Fit a piecewise-linear positive spatial transcript density field}
\usage{
spatial_basis_linear_density_field(
  transcripts_df,
  posterior = NULL,
  return_density = FALSE,
  return_total_density = FALSE,
  return_eta = FALSE,
  store_posterior = FALSE,
  basis = c("3d", "2d", "tri", "bcc"),
  s,
  x = "x_location",
  y = "y_location",
  z = "z_location",
  origin = NULL,
  lambda = 0.1,
  regularization = c("bounded", "quadratic", "huber"),
  delta = 1,
  sigma = 1,
  lambda_laplacian = 0,
  domain_expansion_steps = 0L,
  domain_expansion_axes = c("xyz", "xy"),
  maxit = 100L,
  reltol = 1e-06,
  n_threads = NULL,
  show_progress = TRUE,
  optim = c("summary", "full")
)
}
\arguments{
\item{transcripts_df}{Transcript-level data frame.}

\item{posterior}{Optional numeric transcript-by-cell-type posterior matrix.
If supplied, rows are normalized and used only to decompose the fitted
total density into cell-type-specific densities.}

\item{return_density}{Logical; if \code{TRUE} and \code{posterior} is supplied, return
the transcript-by-cell-type density matrix \code{total_density * posterior}.
Set to \code{FALSE} for large datasets when only basis-level density is needed.}

\item{return_total_density}{Logical; if \code{TRUE}, return the fitted
transcript-level total density vector.}

\item{return_eta}{Logical; if \code{TRUE}, return the fitted transcript-level
log-density vector.}

\item{store_posterior}{Logical; if \code{TRUE}, store the normalized posterior
matrix in the returned object. The posterior is not used for fitting.}

\item{basis}{Character; \code{"2d"}/\code{"tri"} or \code{"3d"}/\code{"bcc"}.}

\item{s}{Positive spatial basis mesh size.}

\item{x, y, z}{Character coordinate column names.}

\item{origin}{Optional lattice origin.}

\item{lambda}{Non-negative smoothing strength on neighboring basis-point
log-density slopes.}

\item{regularization}{Character; one of \code{"quadratic"}, \code{"huber"}, or
\code{"bounded"}.}

\item{delta}{Positive Huber transition scale. Used only when
\code{regularization = "huber"}.}

\item{sigma}{Positive bounded-penalty slope scale. Used only when
\code{regularization = "bounded"}.}

\item{lambda_laplacian}{Non-negative strength for a graph Laplacian
curvature penalty on the basis-point log-density field. This penalizes
deviations from a distance-weighted neighbor average. The supplied value is
scaled internally by \code{s^4} so that the parameter is approximately invariant
to the basis mesh size.}

\item{domain_expansion_steps}{Non-negative integer number of basis-graph
dilation steps used to expand the integration domain beyond basis vertices
touched by transcripts. A value of zero keeps the occupied-domain behavior.}

\item{domain_expansion_axes}{Character; \code{"xyz"} expands along all basis-graph
edges, while \code{"xy"} expands only along edges with no z displacement. The
\code{"xy"} option is mainly useful for thin 3D samples.}

\item{maxit}{Maximum L-BFGS iterations.}

\item{reltol}{Relative convergence tolerance.}

\item{n_threads}{Integer number of OpenMP threads. If \code{NULL}, uses runtime
default.}

\item{show_progress}{Logical; print progress messages.}

\item{optim}{Character; \code{"summary"} stores optimizer diagnostics without the
fitted parameter vector, while \code{"full"} stores the complete \code{\link[stats:optim]{stats::optim()}}
result.}
}
\value{
A list with optional transcript-level density outputs, fitted
basis-level density coefficients, exact simplex domain, basis design,
transcript interpolation design, optimizer diagnostics, and effective
parameters.
}
\description{
Fits an inhomogeneous Poisson point-process density model on the same
triangular or BCC barycentric basis used by \code{\link[=spatial_basis_segmentation]{spatial_basis_segmentation()}}.
Unlike \code{\link[=spatial_basis_log_density_field]{spatial_basis_log_density_field()}}, this model interpolates the
density/intensity itself:
}
\details{
\deqn{
\rho_a = \exp(\alpha_a), \quad
\rho(x) = \sum_a \lambda_a(x) \rho_a.
}

The integral term is exact over occupied simplexes because the integral of a
piecewise-linear basis function is the simplex volume divided by the number
of simplex vertices. Smoothness penalties are applied to the log-density
coefficients \code{alpha}, not to the linear intensities.
}
