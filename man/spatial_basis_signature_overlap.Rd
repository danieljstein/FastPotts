% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basis_watershed_partitioning.R
\name{spatial_basis_signature_overlap}
\alias{spatial_basis_signature_overlap}
\title{Basis-level spatial overlap between segmentation signatures}
\usage{
spatial_basis_signature_overlap(
  segmentation_fit,
  density_fit = NULL,
  cell_type_groups = NULL,
  cell_types = NULL,
  prior_outside = c("nearest", "error"),
  n_threads = NULL
)
}
\arguments{
\item{segmentation_fit}{Result from \code{\link[=spatial_basis_segmentation]{spatial_basis_segmentation()}}.}

\item{density_fit}{Optional result from \code{\link[=spatial_basis_total_density_field]{spatial_basis_total_density_field()}}
or a compatible object containing \code{basis_points} and \code{total_density_basis}.}

\item{cell_type_groups}{Optional named list mapping coarse cell type names to
fine cell-signature names. Fine signatures not listed in any group are
retained as singleton groups before overlap is computed.}

\item{cell_types}{Optional character vector of cell types to include after
grouping. Defaults to all available cell types.}

\item{prior_outside}{Character; how to evaluate the spatial prior for
density-basis vertices whose parent-basis interpolation would require
parent vertices outside \code{segmentation_fit$basis_lattice}. \code{"nearest"} uses
the nearest parent basis vertex logits; \code{"error"} stops.}

\item{n_threads}{Integer number of OpenMP threads for prior interpolation.
If \code{NULL}, uses runtime default.}
}
\value{
A data frame with one row per cell-type pair. \code{shared_mass} is the
density-weighted sum of the pointwise minimum of the two spatial priors.
\code{overlap_coef} is \code{shared_mass / min(mass_a, mass_b)}, so values near 1
mean most of the smaller signature's spatial mass is co-localized with the
other signature. \code{jaccard} is \code{shared_mass / (mass_a + mass_b - shared_mass)}, \code{cosine} is a weighted cosine similarity between prior
fields, and \code{frac_a_shared} / \code{frac_b_shared} are directional shared-mass
fractions.
}
\description{
Computes pairwise overlap between cell-signature spatial prior fields from a
fitted \code{\link[=spatial_basis_segmentation]{spatial_basis_segmentation()}} object. When \code{density_fit} is supplied,
overlap is evaluated on the density basis and weighted by
\code{density_fit$total_density_basis}; otherwise it is evaluated on the
segmentation basis with uniform basis-point weights.
}
\details{
This diagnostic is intended as a cheap pre-partition screen for fine
signatures that occupy the same spatial support and may be better grouped
into a coarser cell type before basis watershed partitioning.
}
