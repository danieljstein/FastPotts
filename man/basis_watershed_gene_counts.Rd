% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basis_watershed_partitioning.R
\name{basis_watershed_gene_counts}
\alias{basis_watershed_gene_counts}
\title{Collect transcript genes into basis-watershed basin counts}
\usage{
basis_watershed_gene_counts(
  basis_partition,
  transcripts_df,
  posterior = NULL,
  gene = "feature_name",
  mode = c("max_posterior", "weighted", "density")
)
}
\arguments{
\item{basis_partition}{Result from \code{\link[=partition_basis_watershed_initial]{partition_basis_watershed_initial()}}.}

\item{transcripts_df}{Transcript-level data frame corresponding to the
density and segmentation fits.}

\item{posterior}{Optional transcript-by-cell-type posterior matrix. Required
for \code{mode = "max_posterior"} and \code{mode = "weighted"}.}

\item{gene}{Character gene column name.}

\item{mode}{Character; \code{"max_posterior"} assigns each transcript once to its
maximum-posterior cell type, \code{"weighted"} contributes posterior weight to
each active cell type with a valid transcript basin, and \code{"density"}
explicitly uses the active cell type with the largest basis-watershed type
density at each transcript's selected mode basis point.}
}
\value{
Sparse \code{dgCMatrix} with genes in rows and \code{celltype-modebasis}
initial watershed cells in columns.
}
\description{
Builds a sparse gene-by-basin count matrix from the transcript assignments
returned by \code{\link[=partition_basis_watershed_initial]{partition_basis_watershed_initial()}}.
}
