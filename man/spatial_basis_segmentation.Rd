% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_basis_segmentation.R
\name{spatial_basis_segmentation}
\alias{spatial_basis_segmentation}
\title{Segment transcripts with a continuous spatial basis model}
\usage{
spatial_basis_segmentation(
  transcripts_df,
  cell_signatures,
  basis = c("3d", "2d", "tri", "bcc"),
  s,
  x = "x_location",
  y = "y_location",
  z = "z_location",
  gene = "feature_name",
  qv = "qv",
  is_gene = "is_gene",
  qv_threshold = 20,
  origin = NULL,
  lambda = 1,
  regularization = c("bounded", "quadratic", "huber"),
  delta = 1,
  sigma = 1,
  purity = c("none", "entropy", "gini"),
  purity_lambda = 0,
  signature_floor = 1e-12,
  normalize_signatures = TRUE,
  refine_signatures = FALSE,
  signature_update_iters = 1L,
  signature_prior_strength = NULL,
  signature_update_rate = 0.25,
  signature_min_posterior = 0,
  maxit = 100L,
  refinement_maxit = NULL,
  reltol = 1e-06,
  n_threads = NULL,
  show_progress = TRUE,
  return_marginals = FALSE,
  return_spatial_prior = FALSE,
  return_logits = FALSE,
  return_max_posterior = FALSE,
  optim_history = c("summary", "full", "none")
)
}
\arguments{
\item{transcripts_df}{A data frame containing transcript-level data.}

\item{cell_signatures}{A numeric matrix with genes in rows and cell types in
columns. Values are treated as gene emission probabilities for each cell
type.}

\item{basis}{Character; either \code{"2d"} for a 2D triangular lattice or \code{"3d"}
for a 3D BCC lattice. The older values \code{"tri"} and \code{"bcc"} are accepted as
aliases.}

\item{s}{Positive numeric mesh size. For \code{basis = "2d"}, this is the side
length of each equilateral Delaunay triangle. For \code{basis = "3d"}, this is
the distance between same-parity BCC lattice points along a coordinate
axis, matching the longest Delaunay tetrahedron edge length.}

\item{x}{Character; column name for x-coordinates.}

\item{y}{Character; column name for y-coordinates.}

\item{z}{Character; column name for z-coordinates. Used only when
\code{basis = "3d"}.}

\item{gene}{Character; column name for gene identifiers.}

\item{qv}{Character; column name for quality values.}

\item{is_gene}{Character; column name indicating whether a transcript should
be treated as a gene. If absent, no gene-only filtering is applied.}

\item{qv_threshold}{Numeric; minimum quality value to retain transcripts.}

\item{origin}{Numeric lattice origin. If \code{NULL}, defaults to the per-axis
median of the filtered transcript coordinates used for fitting.}

\item{lambda}{Non-negative smoothing strength between neighboring basis
coefficients. The transcript likelihood is averaged over transcripts and
the spatial penalty is averaged over graph edges, so \code{lambda} is on an
approximate per-transcript, per-edge spatial-slope scale.}

\item{regularization}{Character; one of \code{"quadratic"}, \code{"huber"}, or
\code{"bounded"}. All options are applied to the spatial logit slope between
neighboring basis points, \code{(w_m - w_n) / edge_distance}. Quadratic
smoothing penalizes squared slope, Huber smoothing is quadratic near zero
and linear past \code{delta}, and bounded smoothing has quadratic small-slope
behavior with asymptotic bound \code{sigma^2}.}

\item{delta}{Positive Huber transition scale in logit units per spatial
unit. Used only when \code{regularization = "huber"}.}

\item{sigma}{Positive bounded-penalty slope scale in logit units per spatial
unit. Used only when \code{regularization = "bounded"}; the per-component
asymptotic penalty is \code{sigma^2}.}

\item{purity}{Character; one of \code{"none"}, \code{"entropy"}, or \code{"gini"}.
Entropy and Gini purity penalties encourage each basis point's cell-type
prior to concentrate on fewer cell types.}

\item{purity_lambda}{Non-negative strength of the basis-point purity penalty.}

\item{signature_floor}{Positive floor applied to signatures before
log-transforming.}

\item{normalize_signatures}{Logical; if \code{TRUE}, normalize each cell-type
signature column after applying \code{signature_floor}.}

\item{refine_signatures}{Logical; if \code{TRUE}, alternates spatial field
fitting with conservative cell-type signature updates from posterior
transcript assignments.}

\item{signature_update_iters}{Integer number of signature update steps. Each
update is followed by a refit of the spatial field.}

\item{signature_prior_strength}{Non-negative Dirichlet pseudo-count strength
for anchoring each refined signature to the input reference signature. If
\code{NULL}, defaults to the number of genes in \code{cell_signatures}.}

\item{signature_update_rate}{Numeric in \verb{[0, 1]}; damping rate for each
signature update.}

\item{signature_min_posterior}{Numeric in \verb{[0, 1]}; if positive, only
posterior assignment weights at least this large contribute to signature
soft counts.}

\item{maxit}{Integer maximum number of L-BFGS iterations for the first
spatial field fit.}

\item{refinement_maxit}{Integer maximum number of L-BFGS iterations for
warm-started spatial field refits after signature updates. If \code{NULL},
uses \code{maxit}.}

\item{reltol}{Approximate relative convergence tolerance. For the
\code{"L-BFGS-B"} optimizer this is converted to \code{factr = reltol / .Machine$double.eps}.}

\item{n_threads}{Integer number of OpenMP threads for objective/gradient and
prediction calculations. If \code{NULL}, uses the OpenMP runtime default.}

\item{show_progress}{Logical; if \code{TRUE}, prints progress messages.}

\item{return_marginals}{Logical; if \code{TRUE}, return the full transcript by
cell-type posterior matrix. Set to \code{FALSE} to avoid allocating and saving
this large derived output.}

\item{return_spatial_prior}{Logical; if \code{TRUE}, return the full fitted
spatial prior matrix at transcript locations.}

\item{return_logits}{Logical; if \code{TRUE}, return the full fitted spatial
logit matrix at transcript locations.}

\item{return_max_posterior}{Logical; if \code{TRUE}, return each transcript's
maximum posterior probability as \code{max_posterior}.}

\item{optim_history}{Character; \code{"summary"} stores per-fit optimizer
diagnostics without repeated parameter vectors, \code{"full"} stores complete
\code{\link[stats:optim]{stats::optim()}} results, and \code{"none"} omits optimizer history.}
}
\value{
A list with components:
\describe{
\item{marginals}{Matrix of posterior transcript probabilities, if
requested with \code{return_marginals = TRUE}.}
\item{spatial_prior}{Matrix of fitted spatial prior probabilities, if
requested with \code{return_spatial_prior = TRUE}.}
\item{logits}{Matrix of fitted spatial logits at transcript locations, if
requested with \code{return_logits = TRUE}.}
\item{max_posterior}{Maximum posterior probability per transcript, if
requested with \code{return_max_posterior = TRUE}.}
\item{basis_weights}{Matrix of fitted centered basis coefficients, one row
per lattice basis point and one column per cell type. Rows sum to zero.}
\item{basis_points}{Matrix of lattice basis point coordinates.}
\item{basis_lattice}{Matrix of integer lattice coordinates.}
\item{basis_edges}{Matrix of neighboring basis point indices and physical
edge distances.}
\item{transcripts_df}{Filtered input data with added \code{label} column.}
\item{optim}{The \code{\link[stats:optim]{stats::optim()}} result.}
\item{optim_history}{List of optimizer summaries by default, one per
spatial field fit.}
\item{cell_signatures_initial}{Input signatures after filtering,
flooring, and optional normalization.}
\item{cell_signatures}{Final signatures used for the returned posterior.}
\item{signature_history}{List of signatures after each refinement step.}
\item{signature_update_history}{List of per-update diagnostics.}
\item{diagnostics}{Preflight size diagnostics for the spatial basis,
optimizer parameter vector, and gradient vector.}
\item{parameters}{Effective basis, optimization, and regularization
parameters used for the fit.}
}
}
\description{
Fits a continuous spatial cell-type field on either a 2D triangular lattice
or a 3D body-centered cubic (BCC) lattice. Each transcript has a spatial
prior over cell types,
\code{softmax(f_1(x_i), ..., f_K(x_i))}, where each \code{f_k(x_i)} is interpolated
from nearby lattice basis coefficients using barycentric weights. The
observed transcript gene then contributes the cell-type signature likelihood.
}
\details{
This implementation performs MAP estimation of the spatial field with
optional conservative refinement of the cell-type signatures. The final
transcript posterior is proportional to the fitted spatial prior multiplied
by the corresponding gene signature probability.
}
\examples{
\dontrun{
fit <- spatial_basis_segmentation(
  transcripts_df,
  cell_signatures,
  basis = "3d",
  s = 2
)
fit$marginals
}

}
