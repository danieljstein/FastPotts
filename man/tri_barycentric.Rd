% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tri_barycentric.R
\name{tri_barycentric}
\alias{tri_barycentric}
\title{Barycentric coordinates on a triangular lattice}
\usage{
tri_barycentric(coords, s, origin = c(0, 0), tol = 1e-10)
}
\arguments{
\item{coords}{Numeric matrix or data frame with two columns containing query
coordinates.}

\item{s}{Positive numeric mesh size. This is the side length of each
equilateral triangle.}

\item{origin}{Numeric vector of length two giving the lattice origin.}

\item{tol}{Numeric tolerance used for triangle boundary checks.}
}
\value{
A list with components:
\describe{
\item{weights}{Numeric matrix with three barycentric weights per row of
\code{coords}.}
\item{points}{Numeric array of dimension \verb{n x 3 x 2} containing the three
active lattice points for each query point.}
\item{lattice}{Integer array of dimension \verb{n x 3 x 2} containing the
triangular-lattice coordinates before multiplying by \code{s} and adding
\code{origin}.}
}
}
\description{
Computes the three active piecewise-linear basis functions for each point on
a triangular lattice. The lattice is generated by basis vectors
\verb{(s, 0)} and \verb{(s / 2, sqrt(3) * s / 2)}, optionally shifted by \code{origin}.
}
\details{
For each query point, the function finds the containing equilateral triangle
and returns its three vertices and barycentric weights. Away from triangle
boundaries exactly three basis functions are non-zero. On edges or vertices,
some returned weights may be zero. The support of each lattice-point basis
function is a regular hexagon.

The Delaunay triangles are equilateral with side length \code{s}. Thus for
\code{s = 1}, each triangle has side length \code{1}. The regular hexagonal support of
each lattice-point basis function has side length \code{s}, vertex-to-vertex
diameter \code{2 * s}, and flat-to-flat diameter \code{sqrt(3) * s}.
}
\examples{
coords <- matrix(c(0.25, 0.25), ncol = 2)
tri <- tri_barycentric(coords, s = 1)
rowSums(tri$weights)

}
