% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile_crf.R
\name{profile_run_crf}
\alias{profile_run_crf}
\title{Profile Memory Usage Across \code{run_crf()} Stages}
\usage{
profile_run_crf(
  transcripts_df,
  cell_signatures,
  x = "x_location",
  y = "y_location",
  z = "z_location",
  gene = "feature_name",
  qv = "qv",
  is_gene = "is_gene",
  qv_threshold = 20,
  n_neighbors = 10,
  dist_threshold = 2,
  same_label_ratio = 5,
  max_iter = 50L,
  damping = 1,
  tol = 0.1,
  num_cores = 6,
  capture_objects = FALSE,
  use_peakRAM = TRUE
)
}
\arguments{
\item{transcripts_df}{A data frame containing transcript-level data.}

\item{cell_signatures}{A numeric matrix (gene x cell types) of probabilities
or likelihoods for each gene within a cell type.}

\item{x}{Character; column name for x-coordinates.}

\item{y}{Character; column name for y-coordinates.}

\item{z}{Character; column name for z-coordinates.}

\item{gene}{Character; column name for gene identifiers.}

\item{qv}{Character; column name for quality values.}

\item{is_gene}{Character; column name marking rows to retain as genes.}

\item{qv_threshold}{Numeric; minimum quality value to retain transcripts.}

\item{n_neighbors}{Integer; number of nearest neighbors to connect.}

\item{dist_threshold}{Numeric; maximum spatial distance for edge inclusion.}

\item{same_label_ratio}{Numeric; same-label ratio used for Potts edge weights.}

\item{max_iter}{Integer; maximum LBP iterations passed to
\code{\link[=potts_lbp_parallel_cpp]{potts_lbp_parallel_cpp()}}.}

\item{damping}{Numeric; damping factor passed to \code{\link[=potts_lbp_parallel_cpp]{potts_lbp_parallel_cpp()}}.}

\item{tol}{Numeric; convergence tolerance passed to \code{\link[=potts_lbp_parallel_cpp]{potts_lbp_parallel_cpp()}}.}

\item{num_cores}{Integer; number of CPU cores to use for parallel inference.}

\item{capture_objects}{Logical; if \code{TRUE}, include stage outputs in the
returned object. Set to \code{FALSE} to avoid retaining large intermediates.}

\item{use_peakRAM}{Logical; if \code{TRUE} and the \code{peakRAM} package is installed,
record stage peak memory usage.}
}
\value{
A list with components:
\item{summary}{Data frame with one row per stage and columns describing
memory usage and output dimensions.}
\item{lbp_estimate_mb}{Approximate lower-bound memory footprint for the
C++ LBP working buffers and returned marginals.}
\item{used_peakRAM}{Logical; whether \code{peakRAM} was used.}
\item{stages}{Named list of per-stage details and, optionally, outputs.}
}
\description{
Profiles the major stages of \code{\link[=run_crf]{run_crf()}} on a given input and reports
stage-level memory usage, output dimensions, and object sizes.
}
\details{
When the optional \code{peakRAM} package is installed, this function records peak
and total RAM usage for each stage. Otherwise it falls back to reporting
stage output sizes and allocation estimates based on \code{Rprofmem()}.
}
\examples{
\dontrun{
prof <- profile_run_crf(transcripts_df, cell_signatures, capture_objects = FALSE)
prof$summary
prof$lbp_estimate_mb
}

}
