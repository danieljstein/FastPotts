% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplex_log_density_integral.R
\name{simplex_log_density_integral}
\alias{simplex_log_density_integral}
\title{Integrate a log-linear density over triangles or tetrahedra}
\usage{
simplex_log_density_integral(
  h,
  volume = 1,
  taylor_radius = 1,
  close_tol = 1e-06,
  taylor_tol = 1e-12,
  taylor_max_terms = 80L,
  gauss_order = 20L,
  n_threads = NULL
)
}
\arguments{
\item{h}{Numeric matrix with one row per simplex and either three columns
for triangles or four columns for tetrahedra. Entries are vertex
log-density values.}

\item{volume}{Numeric scalar or vector with one simplex volume per row of
\code{h}.}

\item{taylor_radius}{Non-negative spread threshold for using the Taylor
expansion.}

\item{close_tol}{Relative pairwise-gap threshold for the Gauss fallback when
the full simplex spread is not small.}

\item{taylor_tol}{Positive Taylor stopping tolerance.}

\item{taylor_max_terms}{Maximum Taylor order.}

\item{gauss_order}{Number of Gauss-Legendre nodes per transformed simplex
dimension for the rare fallback path.}

\item{n_threads}{Integer number of OpenMP threads. If \code{NULL}, uses runtime
default.}
}
\value{
A list with \code{integral}, \code{gradient}, and integer \code{method}, where
method is 0 for divided differences, 1 for Taylor, and 2 for Gauss
fallback.
}
\description{
Computes the integral and vertex-value gradient for a log-linear density on
each simplex:
}
\details{
\deqn{
I(h) = \int_S \exp\left(\sum_i \lambda_i(x) h_i\right) dx.
}

The returned gradient column \code{i} is

\deqn{
\frac{\partial I}{\partial h_i}
=
\int_S \lambda_i(x)
\exp\left(\sum_j \lambda_j(x) h_j\right) dx.
}

The implementation uses a Taylor expansion when vertex values are close, a
shifted divided-difference formula when they are well separated, and a
deterministic Gauss fallback for nearly tied subsets with large spread.
}
