% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{build_potts_lbp_graph_from_edges}
\alias{build_potts_lbp_graph_from_edges}
\title{Build Potts-LBP graph inputs directly from an edge list}
\usage{
build_potts_lbp_graph_from_edges(
  from,
  to,
  weights,
  n_nodes,
  symmetric = FALSE,
  check_reverse = TRUE
)
}
\arguments{
\item{from}{Integer vector of 1-based source node indices.}

\item{to}{Integer vector of 1-based destination node indices.}

\item{weights}{Numeric vector of edge weights. Must either have length 1,
in which case the same weight is used for every input edge, or the same
length as \code{from}.}

\item{n_nodes}{Integer number of nodes in the graph.}

\item{symmetric}{Logical; if \code{TRUE}, each input edge \verb{(u, v)} is treated as
undirected and inserted in both directions \verb{(u, v)} and \verb{(v, u)}.}

\item{check_reverse}{Logical; if \code{TRUE} (default), each directed edge must
have a matching reverse edge after duplicate aggregation.}
}
\value{
A list with the same structure as \code{\link[=build_potts_lbp_graph]{build_potts_lbp_graph()}}.
}
\description{
Builds the CSR-like graph representation expected by \code{potts_lbp()} from a
directed edge list without first materializing a sparse matrix.
}
\details{
Duplicate directed edges are deduplicated so that each directed edge appears
at most once in the returned graph. If duplicate \verb{(from, to)} entries are
supplied with different weights, an error is thrown.

Node indices supplied in \code{from} and \code{to} are \strong{1-based R indices}. Returned
graph indices remain \strong{0-based}, matching the C++ indexing expected by
\code{potts_lbp()}.
}
\keyword{internal}
