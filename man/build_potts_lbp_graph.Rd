% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{build_potts_lbp_graph}
\alias{build_potts_lbp_graph}
\title{Build Potts-LBP graph inputs from a sparse adjacency matrix}
\usage{
build_potts_lbp_graph(mat, drop_diagonal = TRUE, check_reverse = TRUE)
}
\arguments{
\item{mat}{A square sparse matrix of class \code{dgCMatrix}. Off-diagonal nonzero
entries define directed edges; diagonal entries are ignored by default.}

\item{drop_diagonal}{Logical; if \code{TRUE} (default), diagonal entries are
ignored. If \code{FALSE}, diagonal entries are retained, but then each diagonal
entry is its own reverse.}

\item{check_reverse}{Logical; if \code{TRUE} (default), each directed edge must
have a matching reverse edge or an error is thrown.}
}
\value{
A list with components:
\describe{
\item{adj_ptr}{Integer vector of length \code{N + 1} giving CSR row pointers.}
\item{adj_idx}{Integer vector of length \code{E} of 0-based destination nodes.}
\item{rev_idx}{Integer vector of length \code{E}; \code{rev_idx[e]} is the index of
the reverse directed edge for edge \code{e}.}
\item{edge_weights}{Numeric vector of length \code{E} of edge weights in the
same order as \code{adj_idx}.}
\item{n_nodes}{Number of nodes.}
\item{n_edges}{Number of directed edges after optional diagonal removal.}
}
}
\description{
Converts a square sparse adjacency matrix of class \code{dgCMatrix} into the
CSR-like graph representation expected by \code{potts_lbp()}.
}
\details{
The input matrix is interpreted as a directed adjacency matrix stored in
CSC format. If the graph is undirected, each edge should appear twice:
once at \verb{(i, j)} and once at \verb{(j, i)}. The function constructs:
\itemize{
\item \code{adj_ptr}: CSR row pointer array
\item \code{adj_idx}: destination node index for each directed edge
\item \code{rev_idx}: index of the reverse directed edge
\item \code{edge_weights}: edge weights in the same order as \code{adj_idx}
}

Node indices in the returned object are \strong{0-based}, matching the C++
indexing expected by \code{potts_lbp()}.

A \code{dgCMatrix} is stored in compressed sparse column (CSC) format. This helper
first counts outgoing edges per row in order to build a CSR representation,
then fills the directed edge list, and finally computes reverse-edge indices.

Reverse edges are found efficiently by binary-searching for entry \verb{(j, i)}
in column \code{i} for each entry \verb{(i, j)}. For sparse k-NN graphs, this is
typically very fast because each column contains only a small number of
nonzeros.

The returned edge order is by source row, then by the original CSC scan order.
This is suitable for the LBP implementation, which only requires consistency
among \code{adj_ptr}, \code{adj_idx}, \code{rev_idx}, and \code{edge_weights}.
}
\examples{
\dontrun{
library(Matrix)

W <- sparseMatrix(
  i = c(1,2,1,3,2,3),
  j = c(2,1,3,1,3,2),
  x = c(0.7,0.7,0.4,0.4,0.9,0.9),
  dims = c(3,3)
)

g <- build_potts_lbp_graph(W)

str(g)
# g$adj_ptr
# g$adj_idx
# g$rev_idx
# g$edge_weights
}

}
