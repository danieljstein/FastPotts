% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_basis_density_field.R
\name{spatial_basis_density_field}
\alias{spatial_basis_density_field}
\title{Fit a spatial transcript density field with boundary attenuation}
\usage{
spatial_basis_density_field(
  transcripts_df,
  posterior,
  basis = c("3d", "2d", "tri", "bcc"),
  s,
  x = "x_location",
  y = "y_location",
  z = "z_location",
  origin = NULL,
  quadrature_method = c("simplex", "grid"),
  quadrature_subdivision = 4L,
  quadrature_n = 10000L,
  quadrature_expansion = 0.02,
  density_floor = 0.05,
  lambda_eta = 0.1,
  lambda_s_smooth = 0.1,
  lambda_s_prior = 0.1,
  s_prior_mean = -4,
  lambda_edge = 0.25,
  maxit = 100L,
  reltol = 1e-06,
  n_threads = NULL,
  show_progress = TRUE
)
}
\arguments{
\item{transcripts_df}{Transcript-level data frame.}

\item{posterior}{Numeric transcript-by-cell-type posterior probability
matrix.}

\item{basis}{Character; \code{"2d"}/\code{"tri"} or \code{"3d"}/\code{"bcc"}.}

\item{s}{Positive spatial basis mesh size.}

\item{x, y, z}{Character coordinate column names.}

\item{origin}{Optional lattice origin.}

\item{quadrature_method}{Character; \code{"simplex"} uses occupied-simplex
quadrature, while \code{"grid"} uses a regular grid over the coordinate
bounding box.}

\item{quadrature_subdivision}{Positive integer subdivision factor for
occupied-simplex quadrature. In 2D this creates \code{m^2} equal-area
quadrature points per occupied triangle. In 3D this creates \code{m^3}
Duffy-midpoint quadrature points per occupied tetrahedron.}

\item{quadrature_n}{Approximate number of quadrature points over the
coordinate bounding box when \code{quadrature_method = "grid"}.}

\item{quadrature_expansion}{Fractional bounding-box expansion used when
\code{quadrature_method = "grid"}.}

\item{density_floor}{Scalar or vector floor multiplier in \verb{[0, 1)}. A value
of \code{0.05} means boundary attenuation can reduce density to 5 percent of
the baseline.}

\item{lambda_eta}{Smoothness penalty for the baseline log-density field.}

\item{lambda_s_smooth}{Smoothness penalty for the vertex-linear boundary
logit field.}

\item{lambda_s_prior}{L2 shrinkage strength for vertex boundary logits
toward \code{s_prior_mean}.}

\item{s_prior_mean}{Prior mean for vertex boundary logits. Negative values
favor no boundary attenuation.}

\item{lambda_edge}{L2 shrinkage strength for edge-quadratic boundary terms.}

\item{maxit}{Maximum L-BFGS iterations.}

\item{reltol}{Relative convergence tolerance.}

\item{n_threads}{Integer number of OpenMP threads. If \code{NULL}, uses runtime
default.}

\item{show_progress}{Logical; print progress messages.}
}
\value{
A list with fitted parameters, transcript-level predictions,
quadrature design, basis design, optimizer result, and effective
parameters.
}
\description{
Experimental prototype for fitting a posterior-weighted transcript density
field on the same triangular or BCC barycentric basis used by
\code{\link[=spatial_basis_segmentation]{spatial_basis_segmentation()}}. For each cell type, the modeled density is
}
\details{
\deqn{
\rho_k(x) = \exp(\eta_k(x))
\left[f_k + (1 - f_k)\operatorname{sigmoid}(-g_k(x))\right],
}

where \code{eta} is a vertex-linear baseline log-density field, and \code{g} combines
a vertex-linear field with shared edge-quadratic terms,

\deqn{
g_k(x) = \sum_a \lambda_a(x) s_{a,k}
+ \sum_{a<b} \lambda_a(x)\lambda_b(x) s_{ab,k}.
}

The objective is a posterior-weighted inhomogeneous Poisson point-process
likelihood with either occupied-simplex quadrature or a regular-grid
quadrature approximation.
}
