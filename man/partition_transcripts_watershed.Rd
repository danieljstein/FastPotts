% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_partitioning.R
\name{partition_transcripts_watershed}
\alias{partition_transcripts_watershed}
\title{Partition transcripts into putative cells by graph watershed}
\usage{
partition_transcripts_watershed(
  transcripts_df,
  posterior,
  x = "x_location",
  y = "y_location",
  z = "z_location",
  use_z = z \%in\% colnames(transcripts_df),
  n_neighbors = 20L,
  max_distance = Inf,
  density_bandwidth = NULL,
  density_mode = c("type_weighted", "total"),
  distance_weight = 0,
  posterior_weight = 1,
  saddle_ratio_threshold = 0.7,
  posterior_js_threshold = 0.2,
  min_transcripts_per_cell = 5L,
  cell_id = "cell_id",
  unassigned = "UNASSIGNED",
  prior_min_transcripts_per_cell = 10L,
  show_progress = TRUE
)
}
\arguments{
\item{transcripts_df}{Transcript-level data frame.}

\item{posterior}{Numeric matrix of transcript-by-cell-type posterior
probabilities, such as \code{fit$marginals} from
\code{\link[=spatial_basis_segmentation]{spatial_basis_segmentation()}}.}

\item{x, y, z}{Character coordinate column names.}

\item{use_z}{Logical; if \code{TRUE}, include \code{z} in the spatial graph.}

\item{n_neighbors}{Integer number of nearest neighbors for the transcript
graph.}

\item{max_distance}{Numeric maximum graph edge distance.}

\item{density_bandwidth}{Numeric kernel bandwidth. If \code{NULL}, uses the
median graph edge distance.}

\item{density_mode}{Character; \code{"type_weighted"} uses posterior-weighted
type density, while \code{"total"} uses total transcript density.}

\item{distance_weight}{Non-negative penalty for long uphill ascent edges.}

\item{posterior_weight}{Non-negative penalty for posterior divergence during
uphill ascent.}

\item{saddle_ratio_threshold}{Merge adjacent basins when their connecting
saddle is at least this fraction of the smaller mode density.}

\item{posterior_js_threshold}{Maximum Jensen-Shannon divergence allowed for
compatible basin merges.}

\item{min_transcripts_per_cell}{Basins smaller than this are merged into a
compatible neighbor when possible.}

\item{cell_id}{Character prior cell ID column used only for count
calibration diagnostics.}

\item{unassigned}{Character prior cell ID value for unassigned transcripts.}

\item{prior_min_transcripts_per_cell}{Minimum transcript count for reliable
prior cells in calibration diagnostics.}

\item{show_progress}{Logical; print progress messages.}
}
\value{
A list containing transcript assignments, density values, graph
edges, initial basins, basin adjacency diagnostics, merge history, final
cell summaries, and optional prior cell-type count calibration.
}
\description{
Builds a transcript neighborhood graph, estimates local transcript density,
assigns transcripts to density modes by graph ascent, and optionally merges
weakly separated basins using density-saddle and posterior-compatibility
diagnostics.
}
