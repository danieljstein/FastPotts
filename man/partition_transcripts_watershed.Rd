% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_partitioning.R
\name{partition_transcripts_watershed}
\alias{partition_transcripts_watershed}
\title{Partition transcripts into putative cells by graph watershed}
\usage{
partition_transcripts_watershed(
  transcripts_df,
  posterior,
  x = "x_location",
  y = "y_location",
  z = "z_location",
  use_z = z \%in\% colnames(transcripts_df),
  n_neighbors = 20L,
  max_distance = Inf,
  density_bandwidth = NULL,
  density_mode = c("type_weighted", "total"),
  density = NULL,
  distance_weight = 0,
  posterior_weight = 1,
  saddle_ratio_threshold = 0.7,
  posterior_js_threshold = 0.2,
  min_transcripts_per_cell = 5L,
  use_target_counts = TRUE,
  target_cell_type_counts = NULL,
  target_count_sd = NULL,
  target_merge_saddle_ratio = 0.3,
  target_merge_posterior_js = 0.4,
  target_min_transcripts_per_cell = NULL,
  use_size_limits = TRUE,
  cell_size_limits = NULL,
  size_limit_purity_threshold = 0.8,
  size_limit_upper_quantile = 0.99,
  size_limit_min_cells_per_type = 20L,
  size_limit_slack = 1.25,
  cell_id = "cell_id",
  unassigned = "UNASSIGNED",
  prior_min_transcripts_per_cell = 10L,
  show_progress = TRUE
)
}
\arguments{
\item{transcripts_df}{Transcript-level data frame.}

\item{posterior}{Numeric matrix of transcript-by-cell-type posterior
probabilities, such as \code{fit$marginals} from
\code{\link[=spatial_basis_segmentation]{spatial_basis_segmentation()}}.}

\item{x, y, z}{Character coordinate column names.}

\item{use_z}{Logical; if \code{TRUE}, include \code{z} in the spatial graph.}

\item{n_neighbors}{Integer number of nearest neighbors for the transcript
graph.}

\item{max_distance}{Numeric maximum graph edge distance.}

\item{density_bandwidth}{Numeric kernel bandwidth. If \code{NULL}, uses the
median graph edge distance.}

\item{density_mode}{Character; \code{"type_weighted"} uses posterior-weighted
type density, while \code{"total"} uses total transcript density.}

\item{density}{Optional positive numeric vector with one density value per
transcript. If supplied, these values are used directly for graph ascent
and saddle calculations instead of estimating graph-kernel density from
\code{density_bandwidth} and \code{density_mode}.}

\item{distance_weight}{Non-negative penalty for long uphill ascent edges.}

\item{posterior_weight}{Non-negative penalty for posterior divergence during
uphill ascent.}

\item{saddle_ratio_threshold}{Merge adjacent basins when their connecting
saddle is at least this fraction of the smaller mode density.}

\item{posterior_js_threshold}{Maximum Jensen-Shannon divergence allowed for
compatible basin merges.}

\item{min_transcripts_per_cell}{Basins smaller than this are merged into a
compatible neighbor when possible.}

\item{use_target_counts}{Logical; if \code{TRUE}, use estimated or supplied
cell-type target counts to accept additional locally plausible merges that
reduce posterior-weighted count loss.}

\item{target_cell_type_counts}{Optional numeric vector or data frame of
target cell counts by cell type. If \code{NULL}, the function uses the prior
\code{cell_id} calibration when available.}

\item{target_count_sd}{Optional positive scale for each target count. If
\code{NULL}, uses \code{sqrt(max(target, 1))}.}

\item{target_merge_saddle_ratio}{Minimum saddle ratio for target-count-guided
merges.}

\item{target_merge_posterior_js}{Maximum basin posterior Jensen-Shannon
divergence for target-count-guided merges.}

\item{target_min_transcripts_per_cell}{Minimum transcript count for watershed
components to be counted in the target-count merge loss. If \code{NULL}, uses
\code{prior_min_transcripts_per_cell} so prior target estimation and new-cell
counting use the same transcript-count filter.}

\item{use_size_limits}{Logical; if \code{TRUE}, estimate or use type-specific
upper transcript-count limits and block merges that would create
implausibly large cells.}

\item{cell_size_limits}{Optional numeric vector or data frame with
\code{cell_type} and \code{max_transcripts} columns. If \code{NULL}, limits are estimated
from high-purity reliable prior cells when possible.}

\item{size_limit_purity_threshold}{Minimum dominant posterior fraction for a
prior cell to contribute to size-limit estimation.}

\item{size_limit_upper_quantile}{Upper quantile of prior-cell transcript
counts used as the per-type size limit.}

\item{size_limit_min_cells_per_type}{Minimum high-purity prior cells before a
type-specific limit is trusted without shrinkage to the global limit.}

\item{size_limit_slack}{Multiplicative slack applied to type-weighted size
limits during merging.}

\item{cell_id}{Character prior cell ID column used only for count
calibration diagnostics.}

\item{unassigned}{Character prior cell ID value for unassigned transcripts.}

\item{prior_min_transcripts_per_cell}{Minimum transcript count for reliable
prior cells in calibration diagnostics.}

\item{show_progress}{Logical; print progress messages.}
}
\value{
A list containing transcript assignments, density values, graph
edges, initial basins, basin adjacency diagnostics, merge history, final
cell summaries, and optional prior cell-type count calibration.
}
\description{
Builds a transcript neighborhood graph, estimates local transcript density,
assigns transcripts to density modes by graph ascent, and optionally merges
weakly separated basins using density-saddle and posterior-compatibility
diagnostics.
}
